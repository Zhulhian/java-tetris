package tetris;

/**
 * The different block types
 */
public enum SquareType {
    // It is stated as too short, but in this case I think it's enough.
    // Adding javadoc for every single block type seems a bit redundant.
    I, O, T, S, Z, J, L, EMPTY, OUTSIDE
}
